provider "aws" {
  region = "us-west-2"
  profile = "pulse-infra-admin"
}

{% for account in accounts %}
provider "aws" {
  alias  = "{{ account.alias }}"
  region = "us-west-2"
  profile = "pulse-infra-admin"

  assume_role {
    role_arn     = "{{ account.assumable_role_arns }}"
    session_name = "terraform"
  }
}
{% endfor %}

